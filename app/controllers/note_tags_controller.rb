class Note_tagsController < ApplicationController

  def new
  end

  def create
    # params[:note_tags].each do |index, value|
    #   @note_tag.sound_id = params[:sound_id]
    #   @note_tag.user_id = current_user.id
    #   @note_tag.save
    end
  end
end
